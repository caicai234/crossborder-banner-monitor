# 2026-07-01 工作日志

## Banner 监控 — AliExpress 数据修复
- 问题：AliExpress 反爬机制强制跳转登录页，thum.io 截取到登录页而非真实首页，哈希无效
- 尝试1：thum.io 增加 Chrome UA 伪装 + wait/3000 → 失败，仍为登录页
- 尝试2：改为 htmlMode（allorigins.win 代理取 HTML）→ 失败，allorigins 也被屏蔽
- 尝试3：Google Translate 代理 URL 传 thum.io 截图 → **成功**
  - 策略：`https://image.thum.io/get/.../https://translate.google.com/translate?hl=en&sl=en&u=https://www.aliexpress.com/`
  - 三重降级：proxyUrl 截图 → allorigins HTML → manual 标记
- 状态改为 baseline（首次成功建立基准哈希），lastDist=999（与前次 manual 哈希不可比）
- 前端同步 htmlMode 标记，Wayfair 专属文案通用化
- 已推送，GitHub Actions run #5 验证通过

## Banner 监控 — Wayfair 去掉手动模式
- 用户疑问：为何 Wayfair 需要手动检查
- 测试发现 thum.io 可以直接截图 Wayfair（返回 800x400 正常图片），不需要 htmlMode
- 之前误加了 htmlMode:true，导致走 allorigins.win HTML 抓取（必然失败，Cloudflare 520）→ 降级为手动标记
- 修复：去掉 Wayfair 的 htmlMode:true，跟 Amazon/HD/Lowe's 一样走正常 thum.io 截图
- 修改文件：check-once-cloud.js、banner-monitor.html（同步去掉 htmlMode，通用化注释）
- 已推送到 GitHub，下次 GitHub Actions 自动运行时生效

## Banner 监控 — Wayfair 修正与 manual 状态
- 用户反馈：之前去掉 htmlMode 没有真正解决 Wayfair 监控问题
- 重新验证：thum.io 直截 Wayfair 返回 12KB PNG，内容为 Cloudflare "Press & Hold" 人机验证页，无法自动绕过
- 恢复 Wayfair 的 htmlMode:true，并改进 fallback：使用固定 `wayfair-manual-placeholder` 哈希，避免每次时间戳变化导致误报
- 新增 `manual` 状态（UI 显示"需手动"），避免显示为"正常"造成误导
- 在卡片中显示具体原因："Wayfair 启用 Press & Hold 人机验证，当前免费云方案无法自动绕过"
- 已推送到 GitHub
